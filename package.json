{
  "private": true,
  "scripts": {
    "build:css": "for f in assets/css/*.css; do case \"$f\" in *.min.css) continue;; esac; npx cleancss -o \"${f%.css}.min.css\" --source-map \"$f\"; done && echo 'CSS minified.'",
    "build:js": "for f in assets/js/*.js; do case \"$f\" in *.min.js) continue;; esac; npx terser \"$f\" -o \"${f%.js}.min.js\" --compress --mangle --source-map \"url='$(basename ${f%.js}.min.js.map)'\"; done && echo 'JS minified.'",
    "build": "npm run build:css && npm run build:js",
    "lint:css": "stylelint \"assets/css/*.css\" --ignore-pattern \"assets/css/*.min.css\"",
    "lint:css:fix": "stylelint \"assets/css/*.css\" --ignore-pattern \"assets/css/*.min.css\" --fix",
    "lint:js": "eslint \"assets/js/**/*.js\"",
    "lint:js:fix": "eslint \"assets/js/**/*.js\" --fix",
    "test:js": "vitest run",
    "test:js:watch": "vitest",
    "test:js:coverage": "vitest run --coverage",
    "setup-hooks": "git config core.hooksPath .githooks && echo 'Git hooks configured.'"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.8",
    "clean-css-cli": "^5.6.3",
    "eslint": "^10.4.1",
    "globals": "^17.6.0",
    "jquery": "^4.0.0",
    "jsdom": "^29.1.1",
    "stylelint": "^17.13.0",
    "terser": "^5.48.0",
    "vitest": "^4.1.8",
    "stylelint-config-recommended": "^18.0.0"
  }
}